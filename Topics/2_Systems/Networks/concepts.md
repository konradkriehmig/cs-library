How does a new device get an IP address?
What's the difference between TCP and UDP, and when would you use each?
Explain what happens when you type a URL into a browser
What is a load balancer, and how does it decide where to route traffic?
Layer 4 vs Layer 7 load balancing — what's the difference?
What's the difference between a forward proxy and a reverse proxy?
What is DNS and how does resolution work?
REST vs GraphQL vs gRPC — trade-offs?
What's a WebSocket, and how is it different from HTTP polling/long-polling?